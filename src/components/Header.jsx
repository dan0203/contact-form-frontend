const Header = () => {
    return (
        <header>
            <div className="container">
                <h1>Contact Us</h1>
            </div>
        </header>
    );
};

export default Header;
